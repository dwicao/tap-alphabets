import React, { Component } from 'react';
import { View } from 'react-native';
import styles from './index.style';

class TileBlank extends Component {
  render() {
    return (
      <View style={styles.container}/>
    );
  }
}

export default TileBlank;
